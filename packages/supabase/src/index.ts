// @mapgis/supabase — shared Supabase client, auth provider, DB types, Drizzle schema
export { supabase } from "./client";
export * from "./schema";
export { AuthProvider, useAuth } from "./auth/AuthProvider";
